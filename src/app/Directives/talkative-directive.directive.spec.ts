import { TalkativeDirectiveDirective } from './talkative-directive.directive';

describe('TalkativeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TalkativeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
