import { PlaceholderPipePipe } from './placeholder-pipe.pipe';

describe('PlaceholderPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceholderPipePipe();
    expect(pipe).toBeTruthy();
  });
});
